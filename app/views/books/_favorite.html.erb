<turbo-frame id="favorite-button-<%= book.isbn %>">
  <%= button_to book_favorites_path(book),
    id: "js-favorite-button-for-book-#{book.isbn}",
    method: :post,
    local: false,
    data: { type: :script } do %>
    お気に入りに追加☆
  <% end %>
</turbo-frame>