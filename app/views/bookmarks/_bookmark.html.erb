<!--
<tr id="bookmark-<%= bookmark.id %>">
	<td>
		<h3 class="badge badge-info">
			<%= bookmark.user.decorate.user_name %>
		</h3>	
		<div id="js-bookmark-<%= bookmark.id %>">
      <%= simple_format(bookmark.headline) %>
      <%= simple_format(bookmark.body) %>
      <div class="badge badge-success" >
				<%= simple_format(bookmark.page)%>
			</div>
      <%= simple_format(bookmark.chapter) %>
      <%= simple_format(bookmark.link) %>
      <%= bookmark.created_at.to_s(:datetime_jp) %>
    </div>
	</td>
  <% if current_user.own?(bookmark) %>
    <td class="action">
			<ul class="list-inline justify-content-center" style="float: right;">
        <li class="list-inline-item">
					<a href="#" class='js-edit-bookmark-button' data-bookmark-id="<%= bookmark.id %>">
						<button>編集</button>
          </a>
				</li>
				<li class="list-inline-item">
					<a href="#" class='js-delete-bookmark-button' data-bookmark-id="<%= bookmark.id %>">
						<button>削除</button>
					</a>
				</li>
			</ul>
		</td>
  <% end %>
</tr>
-->


<article class="bg-white border-2 border-gray-200 rounded-xl">
  <div class="flex items-start gap-4 p-4 sm:p-6 lg:p-8">
    <a href="#" class="block shrink-0">
      <%= image_tag "bookmark.png", class: "object-cover rounded-lg h-14 w-14" %>
    </a>
    

    <div>

      <!-- 
      <div>
        <% bookmark.tags.each do |tag| %>
          <span class="whitespace-nowrap rounded-full bg-purple-100 px-2.5 py-0.5 text-sm text-purple-700"><%= tag.name %></span>
        <% end %>
      </div>
      -->

		  <h3 class="fomt-medium">
			  <%= bookmark.user.decorate.user_name %>
		  </h3>	

      <h3 class="font-medium sm:text-lg">
        <a class="hover:underline">
          <%= simple_format(bookmark.headline) %>
        </a>
      </h3>

      <p class="text-sm text-gray-700 line-clamp-2">
        <%= simple_format(bookmark.body) %>
        
      </p>

      <div class="mt-2 sm:flex sm:items-center sm:gap-2">
        <div class="flex items-center gap-1 text-gray-500">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="w-4 h-4"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
            stroke-width="2"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M17 8h2a2 2 0 012 2v6a2 2 0 01-2 2h-2v4l-4-4H9a1.994 1.994 0 01-1.414-.586m0 0L11 14h4a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2v4l.586-.586z"
            />
          </svg>

          <p class="text-xs">ぺージ数:</p><%= bookmark.page %></p>
          <p class="text-xs">チャプター/第:</p><%= bookmark.chapter %></p>
        </div>

        <span class="hidden sm:block" aria-hidden="true">&middot;</span>

        <p class="hidden sm:block sm:text-xs sm:text-gray-500">
          参考書籍など:
          <a href="#" class="font-medium underline hover:text-gray-700">
            <%= link_to bookmark.link, bookmark.link %>
          </a>
        </p>

        <div>
          <%= render bookmark.tags %>
        </div>
      </div>
    </div>
  </div>
